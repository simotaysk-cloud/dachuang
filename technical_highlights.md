# 药途寻迹 - 核心业务逻辑与技术亮点详解 (Competition & PPT Supplement)

本内容旨在为大赛文档的【详细设计】与【技术创新】部分提供更具颗粒度的描述，并支撑 PPT 中“扫码开工”与“扫码发运”的工作流展示。

---

## 1. 核心工业流程控制逻辑

### 🟢 1.1 扫码开工 (Scan-to-Start) - 节点的时空保真
*   **业务定义**：作业人员在进入“种植”、“加工”或“质检”环节前，必须通过企业端小程序进行扫码确认。
*   **技术逻辑**：
    1.  **身份与状态校验**：系统通过 JWT 令牌识别当前用户角色（如 Farmer），并校验该批次（Batch）当前是否处于“可开工”前置状态。
    2.  **强制属性采集**：调用微信原生 `wx.getLocation` 获取经纬度，并结合服务器 NTP 时间戳生成“开工快照”。
    3.  **状态机转换**：后端 API 将批次状态由 `CREATED` 变更为 `IN_PROGRESS`，并记录开工流水。
*   **设计价值**：解决了传统溯源中“补录、后录、异地造假”的弊端，实现了全链路“人在现场、时在当下”的数据防篡改起始点。

### 🔵 1.2 扫码发运 (Scan-to-Ship) - 物权与信任的交回
*   **业务定义**：加工完成或出库前，通过扫码完成最终赋码关联与物流去向锁定。
*   **技术逻辑**：
    1.  **GS1-128 工业级合规**：系统按照 GS1 标准动态生成包含 GTIN（产品代码）、Batch No（批次号）以及生产日期的 HRI 与条码。
    2.  **QA 准入卡点**：发运扫码前，系统自动检索该批次是否已关联“质检合格报告（Inspection Passed）”。未过检批次系统将强行拦截发运流程。
    3.  **GS1 锁定 (GS1 Lock)**：一旦执行发运赋码，该批次的生产端数据进入 `READONLY` 状态，确保已流通产品的溯源数据一致性。
*   **设计价值**：将“溯源”从简单的记录工具提升为“质量控制”的硬性卡点。

---

## 2. 三大核心技术亮点

### 🔗 2.1 虚实结合的区块链存证 (EVM Anchor)
*   **机制**：系统不将海量业务明细上链（高成本、低效率），而是采用“哈希锚定”策略。
*   **流程**：将整个批次的各个关键节点（农事记录、质检 Hash）组合成一个数据梅克尔根（Merkle Root），通过 Web3.j/Ethers 调用以太坊智能合约进行 `anchor()` 存证。
*   **结果**：产生不可篡改的 `TxHash`，消费者扫码时可直接链接至区块链浏览器进行核验。

### 🤖 2.2 上下文感知的 AI 智能咨询 (SSE-based AI)
*   **机制**：基于 `Server-Sent Events` 协议的流式对话。
*   **创新点**：
    1.  **自动注入溯源上下文**：当用户在溯源页面发起 AI 提问时，前端会自动将“当前药材名、产地、生长期、质检结果”作为 System Prompt 注入，使得 AI 的回答不仅是通用的知识，更是针对“这根人参”的个性化解读。
    2.  **打字机式平滑体验**：解决移动端弱网环境下大模型响应时间长导致的界面卡死。

### 📏 2.3 GS1 计量单位精密换算算法
*   **挑战**：中药材产地以“箱/筐/斤”计量，工厂以“吨”为单位，药房以“克”销售，换算极易出错。
*   **解决**：系统内置基于国际 GS1 标准的单位换算引擎，支持自动失水率预估与单位对齐，确保溯源过程中的重量守恒，杜绝“洗钱式”虚增库容。

---

## 3. 部署与交付现状 (Tech Status)
*   **生产环境**：已部署至 `cpuzhbc.cn`。
*   **接入安全**：全量 HTTPS 覆盖，通过 Nginx 实现 API 转发与静态资源优化（已解决 `client_max_body_size` 限制问题）。
*   **移动端**：已适配 WeChat Miniprogram 运行环境，核心静态资源（tcm_herb.png 等）已完成压缩优化（<200KB），保障首屏加载速度。
